language: cpp

#whitelist
branches:
  only:
    - master
    - develop

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -y libasound2-dev

script: cd Project/Builds/Linux && ./configure && make && make test

os:
  - linux